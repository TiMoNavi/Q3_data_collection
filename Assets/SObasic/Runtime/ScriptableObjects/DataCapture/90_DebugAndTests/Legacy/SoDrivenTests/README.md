# Legacy SO-Driven Test ScriptableObject Types

These ScriptableObject types are kept only for reference to the older per-test SO runner design.

The current debug design should not add one request/state SO per test case. Use generic SO read/write plus Unity Debug output instead.

Current design document:

```text
Assets/docs/10-so-debug-layer-design.md
```
