# Legacy SO-Driven Test Assets

These assets are old per-test request/state instances.

They are moved out of the active diagnostics folder to avoid confusing the current debug chain.

Do not bind new runtime debug components to these assets.
